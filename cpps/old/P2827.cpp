#include <iostream>
#include <cstdio>
#include <algorithm>
#include <set>
#include <vector>
#include <queue>
#include <stack>
#include <cstdlib>
#include <cstring>
#include <cmath>

using namespace std;
typedef long long ll;
typedef unsigned long long ull;
const int MAXN = 5e4 + 10;
const int INF = 0x3f3f3f3f;
typedef pair<int, int> pii;

priority_queue<int, vector<int>, less<int>> p_q;
int n, m, q, u, v, t;
double p;

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> n >> m >> q >> u >> v >> t;
    p = double(u) / double(v);

    
    return 0;
}